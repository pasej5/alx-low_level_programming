#include "main.h"


int is_palindrome(char *s)
{
	int firstIndex = 0;
	int lastIndex = n -1;

	if (firstIndex >= lastIndex)
		return (1);
	if ([firstIndex] != s[lastIndex])
		return (0);
	return (s, firstIndex + 1, lastIndex + 1);
}
