#include "main.h"

/**
 *positive_or_negative - checks if number is positive or nagative
 *
 * @i: the number to be checked
 *
 * Ruturn: always 0
 */

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative", i);
	else
		printf("%d is positive", i);


}
